# 砂と ひと息

砂時計の内部で砂粒（円形パーティクル）が重力・壁・粒子同士の衝突によって
流れ落ちる様子を再現する、ブラウザ上で動く物理シミュレーションのプロトタイプです。

## 使い方

ビルド不要の素のHTML/CSS/JavaScriptです。`index.html` をブラウザで直接開くか、
簡易サーバーを立てて開いてください。

```bash
# 例: リポジトリ直下で
python3 -m http.server 8000
# ブラウザで http://localhost:8000 を開く
```

「粒のサイズ」で「ふつう／細かい」を選べます。
「砂のつぶ」は、ふつうなら300〜1,000粒（50粒刻み）、細かいなら600〜2,000粒（100粒刻み）です。
サイズ切替時には粒数も倍／半分になり、その場で最初から始まります。
くびれの幅は、ふつうなら12〜80px（2px刻み）、細かいなら半分の6〜40px（1px刻み）です。
粒数スライダーだけを変更した場合は「もういちど」で反映します。
砂時計をクリック・タップするか、「ひっくり返す」で上下を入れ替えられます。
砂時計にフォーカスして Enter / Space キーでも操作できます。
動きを減らす設定では回転アニメーションを省略します。
「くびれの幅」スライダーで内幅を12〜80pxに変更できます。砂の数と現在の配置を引き継ぎ、その場で反映します。

背景は、ディフューザーとリスのぬいぐるみの参照写真をもとにした水彩風イラストです。
生成プロンプトは [assets/README.md](assets/README.md) に記録しています。

## ファイル構成

| ファイル | 役割 |
|---|---|
| `index.html` | キャンバスとUI（スライダー・ボタン・FPS表示） |
| `style.css` | 見た目のスタイル |
| `script.js` | 砂時計の形状定義・物理シミュレーション・描画・UI制御 |

## 粒子数の目安について

実装前に、このPC（WSL2環境）のスペックを確認した上で適切な粒子数を検討しました。

- CPU: AMD Ryzen 7 5800H（8コア16スレッド）
- メモリ: 実機28.9GB（WSL2側の割り当ては14GB）
- GPU: AMD Radeon Graphics（統合、専用GPUメモリ3.0GB + 共有14.5GB）

今回はCanvas2D + CPU上での物理演算という構成のため、GPUの性能は使わず
シングルスレッドJavaScriptでの処理速度が上限を決めます。

素朴な総当たり（全粒子ペアをチェックするN²アルゴリズム）では数百粒子が限界ですが、
このプロトタイプでは**空間分割グリッド**（セルサイズ = 粒子半径の3倍）と、
移動量に応じて更新する近傍ペアのリストを使い、近い粒子だけを衝突判定します。
グリッドとペアは型付き配列を再利用し、壁の曲線は事前計算します。
砂粒は色ごとにまとめて描画し、1粒ずつ描画状態を切り替える負荷を抑えています。
上限は「細かい」の2,000粒です。実際のFPSはブラウザや表示環境に依存するため、
「シミュレーション情報」のFPS表示で確認できます。

## 物理モデルの概要

### 形状

砂時計はコサイン曲線を使った、上下・左右対称の丸い輪郭で表現しています。
描画と壁との衝突判定には同じ境界関数を使います。
高さ（y座標）を与えると、その高さでの内壁の左端/右端のx座標を返す
`leftBoundAt(y)` / `rightBoundAt(y)` を用意し、これを壁との衝突判定に使っています。

### 壁との衝突

各粒子について、`containParticle()` で内壁へのめり込みを補正します。
上下の蓋では高さを制限し、曲面では壁の法線方向へ押し戻して斜面に沿って滑らせます。
くびれを狭くしたときも、粒を水平方向だけに押し込まないようにしています。

また、壁に接触している間は反発処理に加えて、壁沿い方向の速度を減衰させる摩擦
（`WALL_FRICTION_PER_SECOND`）を適用しています。「1秒間接触し続けたら速度が
何割残るか」という形で強さを指定しており、1サブステップにつき1回だけ適用する
ことで、フレームレートやサブステップ数に依存せず一定の強さになるようにしています。

### 粒子同士の衝突と運動量の吸収

60FPSでは基本6回のサブステップに分割し、フレーム間隔や粒子の速度に応じて増やします。

1. 重力と速度から仮の位置を求める
2. 移動量が半径の半分に達した場合だけ近傍ペアを再構築し、床に近い側から粒子同士の重なり補正・壁への補正を12回反復する
3. 補正済みの位置の移動量から速度を求め、圧縮方向の速度が蓄積するのを防ぐ
4. 壁の摩擦と休止判定を適用する

休止中の粒同士も、重なりがあれば位置を補正します。同じ位置にある粒にも
分離方向を与え、重なったまま固定されることを防ぎます。
休止は床・下球の斜面・下側の砂粒に支えられている場合だけ許可し、支えを失った粒や
空中で一時的に遅くなった粒には重力をかけ続けます。

粒子同士がぶつかったとき、相対速度が小さい（ほぼ静止接触に近い）場合は反発させず
運動量をそのまま吸収して止める閾値処理（`REST_VELOCITY_THRESHOLD`）を入れています。
反発係数を掛けて跳ね返すのは、ある程度速い衝突（例: 落下中の粒子が山に飛び込む瞬間）
だけです。

さらに、一定時間（`SLEEP_TIME_REQUIRED`）ほぼ静止し続けた粒子は「休止」状態にし、
重力の適用や自発的な移動を止めます。休止中の粒子は周囲からの衝突を引き続き受けますが、
弱い接触では動かさず、`WAKE_VELOCITY_THRESHOLD` を超える強い衝撃を受けたときだけ
目を覚まして通常どおり動き出します。粒径の4％を超える重なりがある場合にも休止を解除します。
これがないと、下から支える粒子の反力が積み重なった
粒子の上部まで伝わりきらず、山全体がいつまでも微振動し続けてしまいます（反復回数の
少ない接触解決ソルバーに共通する問題です）。休止機構により、砂が落ち切ったあとは
完全に静止し、反転操作などで実際に力が加わったときだけ自然に動き出すようになっています。

### 粒子サイズの動的スケーリング

「ふつう」は従来の粒径計算を維持し、現在の300〜1,000粒の範囲では半径4.5pxです。
「細かい」は対応する通常粒の半径に `1/√2` を掛け、半径約3.182pxとします。
1粒の面積が半分になり、粒数を2倍にすると砂全体の面積を維持できます。

## 今後の拡張案

- 粒子ごとの摩擦係数・反発係数のばらつき（より粒っぽい質感）
- Web Workerによる物理演算の並列化（粒子数上限の引き上げ）
- WebGL/computeシェーダーへの移行（数万粒子規模への対応）
- 落下時間の計測・砂時計としてのタイマー機能

## 動作確認

`node tests/physics.cjs` で、休止粒子の重なり解消、くびれの幅変更、粒子数の保持、反転後の重なり量を確認できます。

`node tests/particle-size.cjs` で、サイズ切替、面積比、粒数範囲、2,000粒での反転・タップ・くびれ変更を検証し、物理計算の中央値と95パーセンタイル時間を表示します（描画時間は含みません）。
